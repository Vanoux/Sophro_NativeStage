{% form_theme form 'bootstrap_4_layout.html.twig' %}

<div class="container mt-3">
{{ form_start(form) }}

    {{ form_widget(form) }}
    <button class="btn btn-success" type="submit">{{ button_label|default('Sauvegarder') }}</button>

{{ form_end(form) }}
</div>
