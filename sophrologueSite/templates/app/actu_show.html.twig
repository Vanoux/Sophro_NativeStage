{% extends 'base.html.twig' %}

{% block title %}Actualités{% endblock %}

{% block body %}

<div class="container m-3">
    <article>
        <h2 class="pb-3">{{ articles.titre }}</h2>
        <div class="metadata">Ecrit le {{ articles.date | date('d/m/Y') }}</div>
        <div class="content">
            <img src="{{ articles.image }}" alt="image d'actualité">
            <p>{{ articles.contenu | raw }}</p>
            <a href="{{ path('actu') }}" class="btn btn-primary btn-sm">Retour à la liste</a>
        </div>
    </article>
</div>

{% endblock %}