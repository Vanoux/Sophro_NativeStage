{% extends 'base.html.twig' %}

{% block title %}Ma Foire à question-Vue-{% endblock %}

{% block body %}
    <h1>Ma Foire à question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Question</th>
                <td>{{ faq.question }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ faq.reponse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('myFaq') }}">Retour à la liste</a>

    <a href="{{ path('faq_edit', {'id': faq.id}) }}">Editer</a>

    {{ include('faq/_delete_form.html.twig') }}
{% endblock %}
